# Slack通知を使う場合は SLACK_CHANNEL_ID を設定する
# チャンネルIDはSlackのチャンネルURLの末尾から確認できる（例: C0XXXXXXXXX）
# export SLACK_CHANNEL_ID="C0XXXXXXXXX"

# 閾値を変更したい場合（省略時は下記デフォルト値）
# export BQ_WARN_GB=1    # 警告閾値 (GB)
# export BQ_BLOCK_GB=2   # ブロック閾値 (GB)
